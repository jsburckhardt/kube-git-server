apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: git-server-claim0
  name: git-server-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
